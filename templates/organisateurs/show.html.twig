{% extends 'base.html.twig' %}

{% block title %}Organisateurs{% endblock %}

{% block body %}
    <h1>Organisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom_organisateur</th>
                <td>{{ organisateur.nomOrganisateur }}</td>
            </tr>
            <tr>
                <th>Num_tel</th>
                <td>{{ organisateur.numTel }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ organisateur.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('organisateurs_index') }}">back to list</a>

    <a href="{{ path('organisateurs_edit', {'id': organisateur.id}) }}">edit</a>

    {{ include('organisateurs/_delete_form.html.twig') }}
{% endblock %}
